# This is a Databricks asset bundle definition for wd36project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: wd36project
  uuid: 8d78d012-4d2f-4ce7-b5f7-1dc785b9345c

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-cbd809d1-6a6c.cloud.databricks.com/
    variables:
      catalog: catalog1_we47
      schema: schema1_we47
  prod:
    mode: production
    workspace:
      host: https://dbc-cbd809d1-6a6c.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/ketanchoudhary1011@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ketanchoudhary1011@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prodcatalog_wd36
      schema: logistics_wd36
    permissions:
      - user_name: ketanchoudhary1011@gmail.com
        level: CAN_MANAGE
